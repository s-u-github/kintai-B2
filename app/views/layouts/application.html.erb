<!DOCTYPE html>  <!-- 使用するHTMLのバージョンをブラウザに対して宣言している -->
<html>
  <head>
    <!-- full_title(カスタムヘルパー) -->
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    
    <%= render 'layouts/shim' %>  <!-- HTML shimがこの１行で済む -->
  </head>

  <body>
    <%= render 'layouts/header' %>  <!--headerがこの１行で済む -->
    <div class="container">
      <% flash.each do |message_type, message| %>  <!-- flashというオブジェクトを対象にflashハッシュキーとバリュー(keyとvalue)を繰り返し処理をする -->
        <div class="alert alert-<%= message_type %>"><%= message %></div>  <!-- alert- ~ のところはCSSのクラスが動的に変化する。例 alert-sucucess -->
      <% end %>
      <%= yield %>  <!-- 各viewの内容がここに挿入される -->
      <%= debug(params) if Rails.env.development? %> <!-- 開発環境でのみデバッグ情報を表示する -->
    </div>
  </body>
</html>
