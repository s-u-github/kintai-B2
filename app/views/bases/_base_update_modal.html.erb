<!-- モーダル・ダイアログ -->
<div class="modal fade" id="base-update-modal" tabindex="-1">
	<div class="modal-dialog modal-sm">
		<div class="modal-content", style="text-align:center;">
			<div class="modal-header">
				<button type="button" class="close" data-dismiss="modal"><span>×</span></button>
				<h4 class="modal-title">拠点情報の編集</h4>
				<div id="modal-base"></div>
			</div>
			
      <div class="modal-body">
        <!--<= form_for @base, url: base_path(), method: :patch do |f| %>-->
        
        <!--  <= f.label :base_number, "拠点番号" %>-->
        <!--  <= f.text_field :base_number, class: 'form-control'%>-->
  
        <!--  <= f.label :base_name, "拠点名" %>-->
        <!--  <= f.text_field :base_name, class: 'form-control' %>-->
       
        <!--  <= f.label :base_info, "勤怠情報" %>-->
        <!--  <= f.text_field :base_info, class: 'form-control' %>-->

        <!--  <= f.submit "保存する", class: "btn btn-primary" %>-->

        <!--< end %><!-- form_for -->-->
      </div> <!-- modal-body -->
    </div> <!-- modal-content -->
  </div> <!-- modal-dialog -->
</div> <!-- modal fade -->

<script>
 $('#base-update-modal').on('show.bs.modal', function (event) { // ボタンがクリックされてモーダルが開いたらイベント発生
 　
  // クリックされたaを取得
  var a = $(event.relatedTarget); 

  // 上記の中の、data-baseの値を取得
  var base = button.data('base'); 

  // クリックされているbase-update-modal要素を取得
  var modal = $(this); 

  // overtime-modalの中のmodal-dateという要素を取得し、そこにdata-dateの値を入れる。これでクリックした日付の日付がモーダル内に入る
  modal.find('#modal-base').text(base);
  
</script>